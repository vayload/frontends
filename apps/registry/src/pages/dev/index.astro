---
import Layout from '../../layouts/Layout.astro';
import { DeveloperDashboard } from '../../components/features/DeveloperDashboard';
import { AppLayout } from '../../layouts/ReactContext.tsx';
---

<Layout title="Developer Dashboard - Vayload Registry">
    <AppLayout client:load client:only="react">
        <DeveloperDashboard client:load client:only="react" />
    </AppLayout>
</Layout>
