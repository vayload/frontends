{
	"name": "vayload-frontends",
	"license": "MIT",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev:landing": "pnpm --filter @vayload/landing dev",
		"dev:registry": "pnpm --filter @vayload/registry dev",
		"build:landing": "pnpm --filter @vayload/landing build",
		"build:registry": "pnpm --filter @vayload/registry build"
	}
}
